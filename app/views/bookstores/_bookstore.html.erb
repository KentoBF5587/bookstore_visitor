<div class="card"> 
  <div class="card-header">
    書店情報No.<%= bookstore.id %>
  </div>
  <div class="card-body">
    <h5 class="card-title"><%= bookstore.name %></h5>
    <p class="card-text"><%= bookstore.address %></p>
    <%= link_to t('bookstores.index.to_bookstore_show'), "#" %> 
  </div>
</div>