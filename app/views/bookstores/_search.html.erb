<%= search_form_for q, url: url do |f| %>
  <div class="input-group mb-3">
    <%= f.search_field :address_or_name_cont, placeholder: "店舗名・地名で絞り込めます", class: "form-control" %>
  <div class="input-group-append">
    <%= f.submit nil, class: "btn btn-success" %>
  </div>
  </div>
<% end %>